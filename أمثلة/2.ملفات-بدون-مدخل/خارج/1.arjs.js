const { translate: __arjs__translate__ } = require('arjs-translate');

{
  let لوحة = 0;
  while (لوحة < 10) {
    لوحة.اطبع(لوحة);
    ++لوحة;
  }
}

{
  let اسم_آخر = 0;
  do {
    لوحة.اطبع(اسم_آخر);
    ++اسم_آخر;
  } while (اسم_آخر < 10);
}